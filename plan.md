# План: 2D-игры (v2.0)

> Статус: обсуждаем. Совместимость: старые скрипты НЕ ломаем, только добавляем.

## 1. Идея

Ncode учится открывать окно Windows и рисовать: чёрный фон по умолчанию,
команды окна и рисования, картинки рядом со скриптом, скаффолд проекта
командой `new структура`, сцены с жизненным циклом. Движок графики —
встроенный Java Swing (Canvas + ImageIO: png, jpg/jpeg, bmp, gif),
новых зависимостей ноль.

## 2. Команды

Окно (одно на все сцены, создаёт `игра.ncode` до перехода на первую):
- `Создать окно [ШИРИНА ВЫСОТА НАЗВАНИЕ]` / `открыть окно` (дефолт: 800×600 `mygame`)
- `Закрыть окно` (= конец программы; крестик — то же; без закрытия окно висит)

Рисование (сцена хранит объекты, экран перерисовывается сам):
- `Нарисовать/рисовать/рисуй [ИМЯ] X Y [РАЗМЕР] [ПОВОРОТ] [R G B] [ПРОЗРАЧНОСТЬ] [ПУТЬ]`
  - первое слово не число → имя объекта; то же имя = обновить (так двигаем)
  - нет пути → квадрат; дефолты: размер 100, поворот 0, чёрный, непрозрачность 100
  - координаты: центр сцены = 0,0; X вправо, Y вверх; X Y — центр объекта
- `Скрыть [ИМЯ]`, `Показать [ИМЯ]` (повторно — ничего; скрытый живёт, не рисуется)
  - без имени — привязанный объект (см. привязку); без привязки — ошибка
  - с именем — любой объект по ИМЕНИ папки в текущей сцене (не по пути!),
    показывает его первый png; привязка не требуется
- `Привязать объект к скрипту` / `привязать <ИМЯ> к скрипту` — явная привязка
  папки к текущему скрипту (перекрывает авто)
- АВТОПРИВЯЗКА (без команды): скрипт внутри папки объекта привязан к ней сам
  (папка скрипта и есть объект)
- Спрайты объекта: `<объект>/Спрайты/*.png` (регистр папки не важен);
  показ берёт первый png по алфавиту (без учёта регистра); нет `Спрайты/` —
  смотрим картинки в корне папки объекта
- `Очистить` (убрать всё со сцены)
- Фон — обычный объект через `запустить скрипт`; кто раньше нарисован, тот ниже
- Анимация существующим: `повтори` + `изменить` + `рисовать` + `ждать 0.05 секунд`
- Позже (не сейчас): клавиатура, мышь, столкновения, поднять/опустить

Сцены:
- `При запуске сцены` / `при старте сцены`, `При выходе из сцены` / `при закрытии сцены`
  (конец не пишем, тело до следующего заголовка/конца файла, верхний уровень)
- Старт: сначала `при запуске`, потом главный поток; закрытие: `при выходе`, потом выход
- `Перейти на сцену ПУТЬ` (`перейти [на] сцену`, `сменить сцену`):
  `при выходе` старой → очистить сцену (окно общее, не трогаем) → загрузить
  (обработчики ЗАМЕНЯЮТ, переменные общие) → `сцена` = путь →
  `при запуске` новой; старый поток останавливается
- Путь как у `запустить` + допопытка `.ncode`; системная переменная `сцена`

## 3. Скаффолд `new структура`

```
какашка/
  игра.ncode          <- только запуск: создать окно (800×600 mygame) + перейти на первую
  компиляция.bat      <- проверка кода + структуры (см. §4)
  Сцена/
    фон/
      fon.ncode       <- заглушка (см. ниже)
      фон.png         <- кидаешь свой рядом (или в Спрайты/)
    какашка2/         <- ОБЪЕКТ = папка
      какашка2.ncode  <- скрипт объекта (автоматом привязан к папке)
      Спрайты/        <- все спрайты объекта (регистр не важен)
        какашка2.png
        какашка2-прыг.png
```
- `игра.ncode` — bootstrap: `создать окно` + `перейти на сцену <первая>`; всё
  остальное (фон, объекты) стартует через `при запуске сцены`.
- Заглушка `fon.ncode` (чёрный фон на чёрном по умолчанию = вся сцена чёрная):
```
При запуске сцены
привязать фон к скрипту
нарисовать фон 0 0 500
```
  Свой фон: кинуть png рядом, дописать путь в конец `рисовать`.

## 4. Проверка структуры (`компиляция.bat`, только если есть `игра.ncode`)

- Ошибка: нет `игра.ncode`; `запустить/перейти` в несуществующий файл;
  в `рисовать` путь к картинке, которой нет рядом с `.ncode`.
- Ошибка: `показать/скрыть <ИМЯ>` — папки с таким именем нет в `Сцена/`.
  (Голая форма без имени не проверяется — привязка видна только в запуске.)
- Предупреждение: папка-объект без `.ncode`; картинка нигде не упомянута.
- Формат тот же: `путь-от-корня:строка: ошибка`.

## 5. Открытые вопросы

1. ~~Заглушка `fon.ncode`, окно, `игра.ncode`~~ — решено.
2. ~~Роль `Спрайты/`~~ — решено: внутри объекта; автопривязка по папке скрипта;
   первый png по алфавиту без учёта регистра; нет `Спрайты/` — корень папки.
3. ~~Автозапуск~~ — решено: НЕТ; старт через `запустить скрипт`; загрузка единообразна
   (сначала `при запуске` файла, потом главный поток).
4. ~~Фон~~ — решено: необязателен, без него чёрный.
5. ~~Привязка~~ — реализовано в v2.0 (явная + авто, первый png по алфавиту).
6. ~~Событийная модель §7~~ — подтверждено: перевзвод по фронту, проверка после
   каждой команды, drain до тишины, дальше окно или выход.

## 6. Этапы

- [x] Этап 1+2: окно, квадраты, картинки, скрыть/показать/очистить/привязать (слиты)
- [x] Этап 3: сцены (`при запуске/выходе`, `перейти`), `сцена`-переменная, события §7
- [x] Этап 4: `new структура` + проверка структуры в компиляции

## 7. Событийная модель v2 (`как только` с перевзводом)

- `как только` НЕ блокирует: регистрирует (условие + тело-«блоки»), поток идёт дальше.
- Проверка условий: после каждой команды + в конце потока + каждый кадр окна;
  порядок регистрации; тело выполняется полностью, потом перепроверка с начала.
- Срабатывание по фронту ложь→истина; первое вычисление: правда → сразу;
  периоды лжи игнорятся («насрать»); перевзвод автомат — каждое новое
  ложь→истина стреляет снова. Пример: истина → сработало → ложь → игнор →
  истина → снова сработало.
- Защита от вечных цепочек: лимит срабатываний за проход, дальше
  `события зациклились` (как у рассылок).
- Жизнь программы: главный поток → drain до тишины (все готовые события) →
  окно открыто? цикл окна (кадры, крестик, `при выходе`) : выход.
  Консоль без окна всегда завершается; `конец` скрипт не убивает —
  убивает только отсутствие слушателей и закрытое окно.
- `Когда будет получено` — как было (стреляет на `вещать`, без перевзвода).

## 8. Бэклог команд из Neocatroid (v1.x, консольное)
Взят в v1.7–v1.9: списки, `пока`, `повторяй/всегда` + `стоп/дальше`,
`синус/косинус` (градусы), `время`, файлы, строки, `вещать` с данными,
списочные формулы.
Следом: JSON (после структур), TryCatch.
Отложено (сложно/сети/телефон): ИИ, Firebase/Firestore/RTDB, HTTP/MQTT/WS,
3D/физика/шейдеры/ML, клоны, камера/GPS/микрофон, перо/движение (к графике v2.0).

## Лог обсуждений

- 2026-09-09: план создан; идея 2D-игр описана и записана.
- 2026-09-09: решения — дефолты, окно с параметрами, сразу видно, координаты от центра, очистить нужна, скелет утверждён.
- 2026-09-09: жизненный цикл сцен отдельными заголовками (не рассылки); старый поток стопается, обработчики заменяются.
- 2026-09-09: объекты-папки в `Сцена/`, форматы картинок, проверка структуры; вопросы §5.
- 2026-09-09: привязка объекта к скрипту, голые/именные показать-скрыть, поиск по имени + первый png.
- 2026-09-09: `игра.ncode` — только bootstrap первой сцены, остальное на `при запуске`.
- 2026-09-09: окно одно на все сцены (дефолт 800×600 `mygame`), создаёт `игра.ncode`.
- 2026-09-09: заглушка `fon.ncode` — привязка + чёрный куб 500 в 0,0.
- 2026-09-09: `Спрайты/` внутри объекта, выбор первого png по алфавиту, автопривязка по папке.
- 2026-09-09: событийная модель v2 — неблокирующее `как только` с перевзводом по фронту, drain до тишины.
- 2026-09-09: событийная модель подтверждена (перевзвод + выход в тишине).
- 2026-09-09: v2.0 готов — окно, рисование, сцены, события, new структура.
- 2026-09-09: РЕФАКТОР 2D v2 — сцены, new структура, привязки и папки снесены. Модель: файлы + `запустить` (чистит экран), объекты по именам, голые команды по последнему нарисованному. Дока: `NCODE_2D_v2.md`.
