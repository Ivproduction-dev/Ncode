# Ncode — документация (v0.1 – v0.7)

> Статус: v0.7 черновик. Файл: `*.ncode`, кодировка UTF-8.

## 1. Принципы

1. Только русский, супер-легкий синтаксис.
2. Одна строка = одна команда.
3. Нет скобок `()`, нет `;`, нет `{}`.
4. Регистр ключевых слов не важен: `Задать` = `задать` = `ЗАДАТЬ`.
5. Пустые строки игнорируются.

## 2. Команды v0.1 (всего 2)

| № | Команда | Алиасы (одно и то же) | Что делает |
|---|---------|------------------------|------------|
| 1 | `задать` | нет | Создает / перезаписывает переменную |
| 2 | вывод | `напечатать`, `печатать`, `вывести` | Как `print` — выводит текст, число или значение переменной |

## 3. Переменные

Имя:
- без кавычек, без скобок, без спецсимволов
- шаблон: буква (а-я ё a-z _) + буквы/цифры/_ 
- примеры: `какашка`, `иван`, `счет1`
- в v0.1 регистр имени игнорируем (все храним в нижнем регистре)

### 3.1 Команда `задать`

Формат:
```
задать <ИМЯ> <ЗНАЧЕНИЕ>
```

`ЗНАЧЕНИЕ` — всё, что после имени до конца строки. Может быть одиночным (см. ниже) или склейкой через `..` (см. 3.3).

Одиночное значение, тип определяем так (порядок важен):

1. `"ИМЯ"` — в двойных кавычках = взять значение из другой переменной
   ```
   задать иван 1
   задать какашка "иван"   # в какашка теперь 1, потому что в иван было 1
   ```
2. `истина` / `ложь` — логический тип
   ```
   задать какашка истина
   ```
3. число — `-?число[.дробь]`
   ```
   задать возраст 25
   задать пи 3.14
   ```
4. всё остальное — строка-литерал как есть (с пробелами)
   ```
   задать имя Иван Петров   # строка "Иван Петров"
   ```

Примеры:
```
задать какашка истина
задать какашка "иван"
задать иван 1
```

### 3.2 Команда вывода

Формат:
```
напечатать <ЗНАЧЕНИЕ>
печатать <ЗНАЧЕНИЕ>
вывести <ЗНАЧЕНИЕ>
```
Все три — одно и то же.

Правила для `<ЗНАЧЕНИЕ>` те же, что у `задать`, плюс склейка `..`:

```
напечатать хеллоу ворлд   # выведет: хеллоу ворлд
напечатать 2              # выведет: 2
напечатать истина         # выведет: истина

задать иван 5
напечатать "иван"         # выведет: 5 (значение переменной иван)
напечатать иван           # выведет: иван (просто текст, без кавычек нет обращения к переменной!)
```

> Важно: без кавычек `иван` = просто слово "иван". С кавычками `"иван"` = значение переменной.

### 3.3 Склейка `..` (соединение)

`..` в движке значит «соединить без лишнего». Работает и в `задать`, и в `напечатать`.

Формат:
```
напечатать <КУСОК> .. <КУСОК> .. <КУСОК>
задать <ИМЯ> <КУСОК> .. <КУСОК>
```

Движок режет выражение строго по `..` и каждый кусок считает по правилам 3.1, потом склеивает.

Твой пример — правильный, пробел там специально, он и есть средний кусок:
```
задать иван Иван
задать нептун Нептун
напечатать "иван".. .."нептун"   # выведет: Иван Нептун
# разбор: ["иван" | " " | "нептун"] -> "Иван" + " " + "Нептун"
```

Но голый пробел невидимый и хрупкий (легко стереть), поэтому рекомендуем слово `пробел`:
```
напечатать "иван" .. пробел .. "нептун"   # выведет: Иван Нептун (так лучше)
напечатать "иван" .. "нептун"             # выведет: ИванНептун (без пробела)
задать полное "иван" .. пробел .. "нептун"
напечатать "полное"                       # Иван Нептун
```

Твой пример с цифрами — да, именно так, `..` всегда склеивает как текст, не складывает:
```
задать иван 1
задать нептун 2
задать полное "иван".."нептун"
напечатать "полное"                       # выведет: 12
задать полное2 "иван"..пробел.."нептун"
напечатать "полное2"                      # выведет: 1 2
```

Правила:
1. Пробелы вокруг `..` в коде игнорируются, в результат не идут.
2. Хочешь пробел в результате — вставь его явно: или `.. ..` (пробел между), или лучше `.. пробел ..`.
3. `пробел` внутри склейки = один `" "`. Вне склейки `пробел` — просто текст "пробел".
4. Пустой кусок = пусто. `..` внутри обычного слова тоже режет: `хеллоу..ворлд` = `хеллоу` + `ворлд`.

## 4. Ошибки v0.1

1. `задать какашка "иван"` а `иван` не существует → `Ошибка: нет переменной "иван" в строке 1`
2. `задать` без имени/значения → `Ошибка: нужно: задать <имя> <значение>`
3. `напечатать` без ничего → `Ошибка: нужно: напечатать <что>`
4. Любой кусок `..` ссылается на несуществующую `"переменную"` → та же ошибка п.1
5. Перезапись разрешена без ошибки.

## 5. Пример программы `пример.ncode`

```
задать иван 1
задать какашка "иван"
напечатать "какашка"
напечатать хеллоу ворлд
напечатать 2
задать флаг истина
напечатать "флаг"
задать нептун 99
напечатать "иван".. .."нептун"
напечатать "иван" .. пробел .. "нептун"
```

Вывод:
```
1
хеллоу ворлд
2
истина
1 99
1 99
```

## 6. Ограничения v0.1 (честно)

- Нельзя вывести буквальный текст `"иван"` с кавычками — он всегда трактуется как переменная. В v0.3 добавим `'...'` или экранирование.
- Нельзя вывести буквальное `..` в тексте — оно всегда режет на куски.
- Пробелы в начале/конце строки обрезаются. Пробел внутри результата — только через `.. ..` или `.. пробел ..`.
- Нет `если`, нет циклов, нет ввода, нет арифметики — это дальше.

---

## 7. v0.2 — сравнения, И / ИЛИ, словоформы да/нет

### 7.1 Словоформы правды и лжи

| Значение | Словоформы (всё одно и то же) |
|----------|-------------------------------|
| истина | `истина`, `да`, `правда` |
| ложь | `ложь`, `нет`, `неправда` |

Печатаем всегда нормализованно — `истина` / `ложь`:
```
напечатать да      # истина
напечатать нет     # ложь
задать флаг правда
напечатать "флаг"  # истина
```

### 7.2 Сравнения

| Оператор | Словоформы |
|----------|------------|
| равно | `равно`, `равняется` |
| не равно | `неравно`, `неравняется` |
| больше | `больше` |
| меньше | `меньше` |
| больше или равно | `больше или равно`, `больше или равняется` |
| меньше или равно | `меньше или равно`, `меньше или равняется` |

```
напечатать 5 равно 5                # истина
напечатать 5 неравно 3              # истина
напечатать 5 больше 3               # истина
напечатать 3 меньше 5               # истина
напечатать 5 больше или равно 5     # истина
напечатать 4 меньше или равно 5     # истина

задать иван 1
задать нептун 2
напечатать "иван" меньше "нептун"   # истина
задать итог "иван" равно "нептун"
напечатать "итог"                   # ложь
```

Как сравниваем:
1. Оба числа → как числа (`10 больше 9` — истина, а не как строки).
2. Оба булевых → `ложь < истина`.
3. Иначе → как строки, регистр не важен (`Иван равно иван` — истина).

### 7.3 И / ИЛИ

```
напечатать истина и истина     # истина
напечатать истина и ложь       # ложь
напечатать ложь или истина     # истина
напечатать 5 больше 3 и 1 меньше 2   # истина
```

Приоритет: `..` > сравнение > `и` > `или`. То есть:
```
напечатать ложь или истина и ложь   # ложь, т.к. это ложь или (истина и ложь)
```

Что считается правдой (для `и`/`или`):
- `истина/да/правда` → да; `ложь/нет/неправда` → нет
- число `0` → нет, любое другое число → да
- любой непустой текст → да, пусто → нет

### 7.4 Правила записи (важно!)

1. Слова-операторы отделяй пробелами: `"иван" меньше "нептун"`. Рядом с кавычками можно вплотную (`"а"или"б"` — ок), но от `..` отделяй пробелом.
2. Только одно сравнение в выражении: `5 равно 5 равно истина` → ошибка.
3. Служебные слова нельзя использовать как имена: `истина да правда ложь нет неправда пробел и или равно равняется неравно неравняется больше меньше`. Движок ругнётся: `` `да` — служебное слово ``.
4. Изменение с v0.1: `напечатать да` теперь выводит `истина` (раньше выводило текст `да`).

### 7.5 Ограничение v0.2 (честно)

Обычный текст, где слово-оператор стоит отдельно (`кот или пес`, `больше всех`), в выражении будет разрезан как оператор. Значения внутри переменных при этом безопасны — подстановка идёт после разбора. В v0.3 решим точными литералами `'...'`.

---

## 8. v0.3 — ЕСЛИ

### 8.1 Формы

Простое (твои примеры, текст без кавычек — обычный текст):
```
если "нептун" равно истина то вывести нептун стал истиной конец
```

С иначе:
```
если "нептун" равно истина то вывести нептун стал истиной иначе вывести нептун не стал истиной конец
```

С иначеесли (можно бесконечно, иначе в конце необязательно):
```
если "нептун" равно истина то вывести нептун стал истиной иначеесли "нептун" равно 3 то вывести нептун стал тройкой иначе вывести нептун непонятный конец
```

Можно в несколько строк (то же самое):
```
если "нептун" равно 3 то
вывести многострочный вариант работает
иначе
вывести не работает
конец
```

Можно вкладывать:
```
если да то если нет то вывести вложенное да иначе вывести вложенное нет конец иначе вывести внешняя ветка конец
```

В условии — любые операторы v0.2 (сравнения, `и`/`или`, склейка):
```
задать возраст 20
если "возраст" больше или равно 18 и "возраст" меньше 65 то вывести взрослый иначе вывести не взрослый конец
если "возраст" .. пробел .. лет равно 20 лет то вывести склейка в условии работает конец
```

### 8.2 Правила

1. Схема: `если У то К (иначеесли У то К)* (иначе К)? конец`. `иначе` — всегда последнее, после него только `конец`.
   Словоформы: `если` = `эсли`, `то` = `тогда`.
2. Условие — любое выражение v0.2 (сравнения, `и`/`или`, склейка). Правда/ложь — по правилам 7.3.
3. В ветке — одна команда (`задать` / `напечатать` / `печатать` / `вывести` / `ждать`) или вложенное `если`. Несколько команд — см. 10 (блоки v0.5).
4. `то` обязательно (словоформа: `тогда` — то же самое).
5. Регистр не важен: `Эсли ... То ... Иначе ... Конец` — работает.
6. Знака `=` нет — пишем словом: `равно 3`, а не `= 3`.
7. Новые служебные слова (нельзя как имена): `если то тогда иначеесли иначе конец`.

### 8.3 Ограничения v0.3 (честно)

- Внутри `если` слова `то/иначе/иначеесли/конец/если` — служебные: обычный текст с ними отдельно стоящими будет разрезан. Ссылки `"..."` при этом безопасны.
- Строка не может начинаться со слов `если`/`конец` (кроме самого `если`).
- Несколько команд в ветке — в v0.5 (блоки).

---

## 9. v0.4 — ЖДАТЬ

Пауза. Формат: `ждать <число> <единица>`.

| Единица | Словоформы |
|---------|------------|
| секунды | `секунда`, `секунды`, `секунд`, `секунду` |
| минуты | `минута`, `минуты`, `минут`, `минуту` |
| часы | `час`, `часа`, `часов` |

```
напечатать старт
ждать 1 секунду
напечатать прошла секунда
ждать 2 секунды
ждать 1 минуту
ждать 2 минуты
ждать 1 час
```

Правила:
1. Число — целое или дробное (`0.5 секунды` = полсекунды), отрицательное — ошибка.
2. Регистр не важен: `Ждать 1 Секунду` — работает.
3. Работает и в ветках `если`: `если да то ждать 1 секунду конец`.
4. Максимум — 24 часа (защита от опечаток вида `ждать 99999 часов`).

---

## 10. v0.5 — БЛОКИ В ЕСЛИ

В ветке — несколько команд, каждая с новой строки:

```
Эсли "имя2" равняется ложь то
ждать 5 секунд
вывести гатово 5 секунд прождали
конец
```

Правила:
1. Вся конструкция в одной строке — как раньше (в ветке одна команда).
2. В многострочном варианте `то` — на той же строке, что условие; команды — со следующих строк.
3. `иначеесли` / `иначе` / `конец` — каждое с новой строки. После команды на той же строке можно закрыть: `вывести готово конец`.
4. Вложенные `если` — можно (однострочные и многострочные).
5. Ошибка внутри ветки показывает точный номер строки команды.

---

## 11. v0.6 — СПРОСИТЬ

Ввод с клавиатуры. Формат: `спросить <подсказка> сохранить в <имя>`.

```
спросить Как тебя зовут сохранить в имя
напечатать привет .. пробел .. "имя"
```

Словоформы разделителя (всё одно и то же): `сохранить в`, `сохранить`, `сохр в`, `сохр`.

```
спросить Сколько тебе лет сохр возраст
спросить пароль сохранить код
```

Правила:
1. Ждёт строку (до Enter) и кладёт в переменную (старое значение затирает).
2. Подсказка необязательна: `спросить сохранить в имя` — просто ждёт молча.
3. В подсказке работают `"переменные"` и `..`. Если подсказка сама содержит слова разделителя — берётся последнее вхождение.
4. Короткий вид без подсказки: `спросить имя`.
5. Имя — по тем же правилам, что у `задать` (служебные слова нельзя).
6. Если ввод закрыт (конец файла) — кладётся пусто.
7. Работает и в ветках `если`.

---

## 12. Запуск на Windows

```
Ncode программа.ncode
Ncode -help
Ncode программа.ncode < ввод.txt   — подсунуть ввод из файла
```

Движок сам включает UTF-8 в интерактивной консоли. Если видишь кракозябры — выполни разово `chcp 65001` (или используй Windows Terminal).

---

## 13. v0.7 — ПОВТОРИ

Цикл. Формат: `повтори <число> раз`, тело — команды до `конец`.

```
повтори 3 раза
напечатать привет
конец
```

Словоформы команды: `повтори`, `повторить`, `повторять`. Счёт: `раз`, `раза`, `разов`.

```
повтори 2 раза напечатать быстро конец
задать мало 2
повторить "мало" раз
напечатать из переменной
конец
```

Правила:
1. Счёт — целое неотрицательное число, можно из переменной. `0 раз` — тело пропускается.
2. Вложенность можно: `повтори` внутри `повтори` и `если`, `если` внутри `повтори`.
3. Первая ошибка в теле останавливает повторы.
4. Максимум — 100000 повторов (защита от зависания).
5. Служебные слова (нельзя как имена): `повтори повторить повторять`.
6. Ограничение: голое слово `повтори` в тексте однострочного `если` с `иначе` — только через блочную форму. Однострочный `повтори` внутри однострочного `если` — работает: `если да то повтори 2 раза вывести а конец конец`.
