# Ncode 2D v2 — документация

> Статус: версия 2D v2. Один файл `игра.ncode`. Запуск: `Ncode игра.ncode`.

## Проект

```
нарисовать герой 0 0
```

Вся игра — один скрипт: сначала `создать окно`, дальше объекты и игра.

## Окно

```
создать окно 800 600 мояигра
закрыть окно
```

Без параметров — 800×600 `mygame`. Окно одно. `Закрыть окно` и крестик
завершают программу. Без закрытия окно висит.

## Объекты

```
нарисовать герой 0 0
нарисовать куб 50 50
задать размер 60 объекту куб
задать цвет 0 255 0 объекту куб
задать образ объекту герой герой.png
```

Формат: `нарисовать [ИМЯ] X Y`.
Первое слово не число → имя; то же имя = переместить (обновить X Y). Центр 0,0
(X вправо, Y вверх); X Y — центр объекта. Новое — всегда квадрат 100 чёрный.
Без имени — один общий объект.
Размер, поворот, цвет, прозрачность, картинка — отдельно:
`задать размер/поворот/цвет/прозрачность/образ объекту <имя>`.

```
показать герой
скрыть герой
показать
очистить
```

С именем — нарисованный объект; голые — последний нарисованный.
Очистка экрана — `очистить`.

## Движение (по последнему нарисованному)

```
идти 10 шагов
повернуть налево на 15 градусов
повернуть направо на 15
плыть к 100 50 за 2 секунды
```

Угол объекта: 0 смотрит вправо, 90 — вверх. `идти` — вперёд по углу
(назад — отрицательным). `плыть` ждёт конца (как `ждать`).

## Ввод

```
Когда нажата клавиша а
напечатать нажали а

Когда нажата клавиша пробел
напечатать пробел

При нажатии
напечатать клик

При нажатии на герой
напечатать попал
```

Клавиша одним словом: русские/английские буквы, цифры, спецсимволы,
`пробел`, `ввод`, стрелки (`влево`...), `esc`, `таб` и др. Клик мимо
объекта — только голый обработчик. Координаты клика — `мышьх`, `мышьу`.
Без окна ввод не работает (окно кликни, чтобы фокус был в нём).
