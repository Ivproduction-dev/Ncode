# Ncode 2D v2 — документация

> Статус: версия 2D v2. Проект игры: папка с любым именем, внутри `main.ncode`
> и `settings.ncode`. Запуск: `Ncode путь/main.ncode`.

## Проект

```
мояигра/
  main.ncode       <- главный: запустить скрипт settings.ncode, дальше игра
  settings.ncode   <- настройки: создать окно 800 600 мояигра
```

`settings.ncode` — обычный скрипт (`создать окно` и др.),
запускается первой строкой `main.ncode` через `запустить скрипт`.

## Окно

```
создать окно 800 600 мояигра
закрыть окно
```

Без параметров — 800×600 `mygame`. Окно одно. `Закрыть окно` и крестик
завершают программу. Без закрытия окно висит.

## Объекты

```
нарисовать герой 0 0 100
нарисовать куб 50 50 60 0 255 0 0 100
```

Формат: `нарисовать [ИМЯ] X Y [РАЗМЕР] [ПОВОРОТ] [R G B] [ПРОЗРАЧНОСТЬ] [ПУТЬ]`.
Первое слово не число → имя; то же имя = обновить. Центр сцены 0,0
(X вправо, Y вверх); X Y — центр объекта. Дефолты: 100, 0, чёрный, 100.
Без имени — один общий объект. Нет пути → квадрат.

```
показать герой
скрыть герой
показать
очистить
```

С именем — нарисованный объект; голые — последний нарисованный.
`запустить` чужой скрипт чистит экран (если окно открыто).

## Движение (по последнему нарисованному)

```
идти 10 шагов
повернуть налево на 15 градусов
повернуть направо на 15
плыть к 100 50 за 2 секунды
```

Угол объекта: 0 смотрит вправо, 90 — вверх. `идти` — вперёд по углу
(назад — отрицательным). `плыть` ждёт конца (как `ждать`).

## Ввод

```
Когда нажата клавиша а
напечатать нажали а

Когда нажата клавиша пробел
напечатать пробел

При нажатии
напечатать клик

При нажатии на герой
напечатать попал
```

Клавиша одним словом: русские/английские буквы, цифры, спецсимволы,
`пробел`, `ввод`, стрелки (`влево`...), `esc`, `таб` и др. Клик мимо
объекта — только голый обработчик. Координаты клика — `мышьх`, `мышьу`.
Без окна ввод не работает (окно кликни, чтобы фокус был в нём).
