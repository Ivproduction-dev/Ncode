# Ncode — документация (v0.1 – 2.1)

> Статус: версия 2.1. Файл: `*.ncode`, кодировка UTF-8.

## 1. Принципы

1. Только русский, супер-легкий синтаксис.
2. Одна строка = одна команда.
3. Нет скобок `()`, нет `;`, нет `{}`.
4. Регистр ключевых слов не важен: `Задать` = `задать` = `ЗАДАТЬ`.
5. Пустые строки игнорируются.

## 2. Команды v0.1 (всего 2)

| № | Команда | Алиасы (одно и то же) | Что делает |
|---|---------|------------------------|------------|
| 1 | `задать` | нет | Создает новую или перезаписывает (старое стирается) |
| 1б | `изменить` | `поменять` | Меняет СУЩЕСТВУЮЩУЮ (если нет — ошибка) |
| 2 | вывод | `напечатать`, `печатать`, `вывести` | Как `print` — выводит текст, число или значение переменной |

## 3. Переменные

Имя:
- без кавычек, без скобок, без спецсимволов
- шаблон: буква (а-я ё a-z _) + буквы/цифры/_ 
- примеры: `какашка`, `иван`, `счет1`
- в v0.1 регистр имени игнорируем (все храним в нижнем регистре)

### 3.1 Команда `задать` — создать или перезаписать

Формат:
```
задать <ИМЯ> <ЗНАЧЕНИЕ>
```

Создаёт новую, а если имя уже есть — старое значение выкидывается и пишется новое.

`ЗНАЧЕНИЕ` — всё, что после имени до конца строки. Может быть одиночным (см. ниже) или склейкой через `..` (см. 3.3).

Одиночное значение, тип определяем так (порядок важен):

1. `"ИМЯ"` — в двойных кавычках = взять значение из другой переменной
   ```
   задать иван 1
   задать какашка "иван"   # в какашка теперь 1, потому что в иван было 1
   ```
2. `истина` / `ложь` — логический тип
   ```
   задать какашка истина
   ```
3. число — `-?число[.дробь]`
   ```
   задать возраст 25
   задать пи 3.14
   ```
4. всё остальное — строка-литерал как есть (с пробелами)
   ```
   задать имя Иван Петров   # строка "Иван Петров"
   ```

Примеры:
```
задать какашка истина
задать какашка "иван"
задать иван 1
```

### 3.2 Команда вывода

Формат:
```
напечатать <ЗНАЧЕНИЕ>
печатать <ЗНАЧЕНИЕ>
вывести <ЗНАЧЕНИЕ>
```
Все три — одно и то же.

Правила для `<ЗНАЧЕНИЕ>` те же, что у `задать`, плюс склейка `..`:

```
напечатать хеллоу ворлд   # выведет: хеллоу ворлд
напечатать 2              # выведет: 2
напечатать истина         # выведет: истина

задать иван 5
напечатать "иван"         # выведет: 5 (значение переменной иван)
напечатать иван           # выведет: иван (просто текст, без кавычек нет обращения к переменной!)
```

> Важно: без кавычек `иван` = просто слово "иван". С кавычками `"иван"` = значение переменной.

### 3.3 Склейка `..` (соединение)

`..` в движке значит «соединить без лишнего». Работает и в `задать`, и в `напечатать`.

Формат:
```
напечатать <КУСОК> .. <КУСОК> .. <КУСОК>
задать <ИМЯ> <КУСОК> .. <КУСОК>
```

Движок режет выражение строго по `..` и каждый кусок считает по правилам 3.1, потом склеивает.

Твой пример — правильный, пробел там специально, он и есть средний кусок:
```
задать иван Иван
задать нептун Нептун
напечатать "иван".. .."нептун"   # выведет: Иван Нептун
# разбор: ["иван" | " " | "нептун"] -> "Иван" + " " + "Нептун"
```

Но голый пробел невидимый и хрупкий (легко стереть), поэтому рекомендуем слово `пробел`:
```
напечатать "иван" .. пробел .. "нептун"   # выведет: Иван Нептун (так лучше)
напечатать "иван" .. "нептун"             # выведет: ИванНептун (без пробела)
задать полное "иван" .. пробел .. "нептун"
напечатать "полное"                       # Иван Нептун
```

Твой пример с цифрами — да, именно так, `..` всегда склеивает как текст, не складывает:
```
задать иван 1
задать нептун 2
задать полное "иван".."нептун"
напечатать "полное"                       # выведет: 12
задать полное2 "иван"..пробел.."нептун"
напечатать "полное2"                      # выведет: 1 2
```

Правила:
1. Пробелы вокруг `..` в коде игнорируются, в результат не идут.
2. Хочешь пробел в результате — вставь его явно: или `.. ..` (пробел между), или лучше `.. пробел ..`.
3. `пробел` внутри склейки = один `" "`. Вне склейки `пробел` — просто текст "пробел".
4. Пустой кусок = пусто. `..` внутри обычного слова тоже режет: `хеллоу..ворлд` = `хеллоу` + `ворлд`.
5. `точкаточка` внутри склейки = буквально `..`. Вне склейки `точкаточка` — просто текст. Пример: `напечатать а .. точкаточка .. б` выведет `а..б`.

## 4. Ошибки v0.1

1. `задать какашка "иван"` а `иван` не существует → `Ошибка: нет переменной "иван" в строке 1`
2. `задать` без имени/значения → `Ошибка: нужно: задать <имя> <значение>`
3. `напечатать` без ничего → `Ошибка: нужно: напечатать <что>`
4. Любой кусок `..` ссылается на несуществующую `"переменную"` → та же ошибка п.1
5. Перезапись разрешена без ошибки.

## 5. Пример программы `пример.ncode`

```
задать иван 1
задать какашка "иван"
напечатать "какашка"
напечатать хеллоу ворлд
напечатать 2
задать флаг истина
напечатать "флаг"
задать нептун 99
напечатать "иван".. .."нептун"
напечатать "иван" .. пробел .. "нептун"
```

Вывод:
```
1
хеллоу ворлд
2
истина
1 99
1 99
```

## 6. Ограничения v0.1 (честно)

- Нельзя вывести буквальный текст `"иван"` с кавычками — он всегда трактуется как переменная. В v0.3 добавим `'...'` или экранирование.
- Нельзя вывести буквальное `..` в тексте — оно всегда режет на куски.
- Пробелы в начале/конце строки обрезаются. Пробел внутри результата — только через `.. ..` или `.. пробел ..`.
- Нет `если`, нет циклов, нет ввода, нет арифметики — это дальше.

---

## 7. v0.2 — сравнения, И / ИЛИ, словоформы да/нет

### 7.1 Словоформы правды и лжи

| Значение | Словоформы (всё одно и то же) |
|----------|-------------------------------|
| истина | `истина`, `да`, `правда` |
| ложь | `ложь`, `нет`, `неправда` |

Печатаем всегда нормализованно — `истина` / `ложь`:
```
напечатать да      # истина
напечатать нет     # ложь
задать флаг правда
напечатать "флаг"  # истина
```

### 7.2 Сравнения

| Оператор | Словоформы |
|----------|------------|
| равно | `равно`, `равняется` |
| не равно | `неравно`, `неравняется` |
| больше | `больше` |
| меньше | `меньше` |
| больше или равно | `больше или равно`, `больше или равняется` |
| меньше или равно | `меньше или равно`, `меньше или равняется` |

```
напечатать 5 равно 5                # истина
напечатать 5 неравно 3              # истина
напечатать 5 больше 3               # истина
напечатать 3 меньше 5               # истина
напечатать 5 больше или равно 5     # истина
напечатать 4 меньше или равно 5     # истина

задать иван 1
задать нептун 2
напечатать "иван" меньше "нептун"   # истина
задать итог "иван" равно "нептун"
напечатать "итог"                   # ложь
```

Как сравниваем:
1. Оба числа → как числа (`10 больше 9` — истина, а не как строки).
2. Оба булевых → `ложь < истина`.
3. Иначе → как строки, регистр не важен (`Иван равно иван` — истина).

### 7.3 И / ИЛИ

```
напечатать истина и истина     # истина
напечатать истина и ложь       # ложь
напечатать ложь или истина     # истина
напечатать 5 больше 3 и 1 меньше 2   # истина
```

Приоритет: `..` > сравнение > `и` > `или`. То есть:
```
напечатать ложь или истина и ложь   # ложь, т.к. это ложь или (истина и ложь)
```

Что считается правдой (для `и`/`или`):
- `истина/да/правда` → да; `ложь/нет/неправда` → нет
- число `0` → нет, любое другое число → да
- любой непустой текст → да, пусто → нет

### 7.4 Правила записи (важно!)

1. Слова-операторы отделяй пробелами: `"иван" меньше "нептун"`. Рядом с кавычками можно вплотную (`"а"или"б"` — ок), но от `..` отделяй пробелом.
2. Только одно сравнение в выражении: `5 равно 5 равно истина` → ошибка.
3. Служебные слова нельзя использовать как имена: `истина да правда ложь нет неправда пробел и или равно равняется неравно неравняется больше меньше`. Движок ругнётся: `` `да` — служебное слово ``.
4. Изменение с v0.1: `напечатать да` теперь выводит `истина` (раньше выводило текст `да`).

### 7.5 Ограничение v0.2 (честно)

Обычный текст, где слово-оператор стоит отдельно (`кот или пес`, `больше всех`), в выражении будет разрезан как оператор. Значения внутри переменных при этом безопасны — подстановка идёт после разбора. В v0.3 решим точными литералами `'...'`.

---

## 8. v0.3 — ЕСЛИ

### 8.1 Формы

Простое (твои примеры, текст без кавычек — обычный текст):
```
если "нептун" равно истина то вывести нептун стал истиной конец
```

С иначе:
```
если "нептун" равно истина то вывести нептун стал истиной иначе вывести нептун не стал истиной конец
```

С иначеесли (можно бесконечно, иначе в конце необязательно):
```
если "нептун" равно истина то вывести нептун стал истиной иначеесли "нептун" равно 3 то вывести нептун стал тройкой иначе вывести нептун непонятный конец
```

Можно в несколько строк (то же самое):
```
если "нептун" равно 3 то
вывести многострочный вариант работает
иначе
вывести не работает
конец
```

Можно вкладывать:
```
если да то если нет то вывести вложенное да иначе вывести вложенное нет конец иначе вывести внешняя ветка конец
```

В условии — любые операторы v0.2 (сравнения, `и`/`или`, склейка):
```
задать возраст 20
если "возраст" больше или равно 18 и "возраст" меньше 65 то вывести взрослый иначе вывести не взрослый конец
если "возраст" .. пробел .. лет равно 20 лет то вывести склейка в условии работает конец
```

### 8.2 Правила

1. Схема: `если У то К (иначеесли У то К)* (иначе К)? конец`. `иначе` — всегда последнее, после него только `конец`.
   Словоформы: `если` = `эсли`, `то` = `тогда`.
2. Условие — любое выражение v0.2 (сравнения, `и`/`или`, склейка). Правда/ложь — по правилам 7.3.
3. В ветке — одна команда (`задать` / `напечатать` / `печатать` / `вывести` / `ждать`) или вложенное `если`. Несколько команд — см. 10 (блоки v0.5).
4. `то` обязательно (словоформа: `тогда` — то же самое).
5. Регистр не важен: `Эсли ... То ... Иначе ... Конец` — работает.
6. Знака `=` нет — пишем словом: `равно 3`, а не `= 3`.
7. Новые служебные слова (нельзя как имена): `если то тогда иначеесли иначе конец`.

### 8.3 Ограничения v0.3 (честно)

- Внутри `если` слова `то/иначе/иначеесли/конец/если` — служебные: обычный текст с ними отдельно стоящими будет разрезан. Ссылки `"..."` при этом безопасны.
- Строка не может начинаться со слов `если`/`конец` (кроме самого `если`).
- Несколько команд в ветке — в v0.5 (блоки).

---

## 9. v0.4 — ЖДАТЬ

Пауза. Формат: `ждать <число> <единица>`.

| Единица | Словоформы |
|---------|------------|
| секунды | `секунда`, `секунды`, `секунд`, `секунду` |
| минуты | `минута`, `минуты`, `минут`, `минуту` |
| часы | `час`, `часа`, `часов` |

```
напечатать старт
ждать 1 секунду
напечатать прошла секунда
ждать 2 секунды
ждать 1 минуту
ждать 2 минуты
ждать 1 час
```

Правила:
1. Число — целое или дробное (`0.5 секунды` = полсекунды), отрицательное — ошибка.
2. Регистр не важен: `Ждать 1 Секунду` — работает.
3. Работает и в ветках `если`: `если да то ждать 1 секунду конец`.
4. Максимум — 24 часа (защита от опечаток вида `ждать 99999 часов`).

---

## 10. v0.5 — БЛОКИ В ЕСЛИ

В ветке — несколько команд, каждая с новой строки:

```
Эсли "имя2" равняется ложь то
ждать 5 секунд
вывести гатово 5 секунд прождали
конец
```

Правила:
1. Вся конструкция в одной строке — как раньше (в ветке одна команда).
2. В многострочном варианте `то` — на той же строке, что условие; команды — со следующих строк.
3. `иначеесли` / `иначе` / `конец` — каждое с новой строки. После команды на той же строке можно закрыть: `вывести готово конец`.
4. Вложенные `если` — можно (однострочные и многострочные).
5. Ошибка внутри ветки показывает точный номер строки команды.

---

## 17. v0.10 — ИЗМЕНИТЬ

Меняет СУЩЕСТВУЮЩУЮ переменную. Формы: `изменить`, `поменять`.

```
задать счёт 0
напечатать "счёт"      # 0
изменить счёт 5
напечатать "счёт"      # 5
поменять счёт "счёт" плюс 1
напечатать "счёт"      # 6
```

Правила:
1. `задать` — создать новую или молча перезаписать. `изменить` — только поменять существующую (нет такой — ошибка ``нет `имя` — сначала `задать` ``).
2. Значение — любое выражение (склейка, арифметика, сравнения).
3. `спросить ... сохранить в ...` — как было: создаёт или перезаписывает.

---

## 11. v0.6 — СПРОСИТЬ

Ввод с клавиатуры. Формат: `спросить <подсказка> сохранить в <имя>`.

```
спросить Как тебя зовут сохранить в имя
напечатать привет .. пробел .. "имя"
```

Словоформы разделителя (всё одно и то же): `сохранить в`, `сохранить`, `сохр в`, `сохр`.

```
спросить Сколько тебе лет сохр возраст
спросить пароль сохранить код
```

Правила:
1. Ждёт строку (до Enter) и кладёт в переменную (старое значение затирает).
   Подсказка печатается без перевода строки + один пробел в конце.
2. Подсказка необязательна: `спросить сохранить в имя` — просто ждёт молча.
3. В подсказке работают `"переменные"` и `..`. Если подсказка сама содержит слова разделителя — берётся последнее вхождение.
4. Короткий вид без подсказки: `спросить имя`.
5. Имя — по тем же правилам, что у `задать` (служебные слова нельзя).
6. Если ввод закрыт (конец файла) — кладётся пусто.
7. Работает и в ветках `если`.

---

## 12. Запуск на Windows

```
Ncode программа.ncode
Ncode -help
Ncode программа.ncode < ввод.txt   — подсунуть ввод из файла
```

Движок сам включает UTF-8 в интерактивной консоли. Если видишь кракозябры — выполни разово `chcp 65001` (или используй Windows Terminal). Если вместо букв знаки `?` — смени шрифт консоли на Lucida Console (свойства окна → шрифт), точечные шрифты кириллицу не умеют. Файлы с BOM (так сохраняет Блокнот) читаются нормально.

---

## 14. v0.8 — АРИФМЕТИКА

Слова-операторы (регистр не важен, отделяй пробелами):

| Оператор | Словоформы | Пример | Ответ |
|----------|------------|--------|-------|
| плюс | `плюс` | `2 плюс 3` | `5` |
| минус | `минус` | `10 минус 4` | `6` |
| умножить | `умножить` | `3 умножить 4` | `12` |
| разделить | `разделить`, `поделить` | `10 разделить 4` | `2.5` |
| остаток | `остаток` | `10 остаток 3` | `1` |

```
напечатать 2 плюс 3 умножить 4   # 14 (умножение раньше)
напечатать 5 плюс 3 равно 8      # истина
задать цена 100
задать скидка 30
напечатать "цена" минус "скидка" # 70
напечатать минус 5 плюс 10       # 5
```

Приоритет: `..` > `умножить/разделить/остаток` > `плюс/минус` > сравнение > `и` > `или`.

Правила:
1. Операнды — числа или переменные с числами, иначе ошибка `тут нужно число`.
2. Деление показывает дробь (`7 разделить 2` = `3.5`), целое — без точки (`6 разделить 2` = `3`).
3. На ноль делить нельзя (и остаток от нуля тоже).
4. Счёт в `повтори` и число в `ждать` понимают выражения: `повтори 2 плюс 3 раз`, `ждать "пауза" секунду`.
5. Служебные слова (нельзя как имена): `плюс минус умножить разделить поделить остаток`.
6. Изменение: голые слова `плюс/минус/умножить/...` в выражениях теперь операторы, а не текст.

---

## 15. v0.8/v2.0 — КАК ТОЛЬКО (событие)

Не блокирует: регистрирует условие и отпускает поток. Проверка после каждой
команды, в конце потока и каждый кадр окна:

```
как только 2 плюс 2 равно 4 то вывести событие сработало конец
```

Блочная форма:

```
как только "готово" равно да то
вывести многострочное событие
вывести сработало сразу
конец
```

Правила:
1. Условие — любое выражение (сравнения, `и`/`или`, арифметика, склейка).
2. Срабатывание по фронту ложь→истина; первая правда — сразу; ложь — игнор.
3. Перевзвод автомат: каждое новое ложь→истина стреляет снова.
4. `иначе` тут нет — только условие, команды и `конец`.
5. Вложенность работает в обе стороны.
6. Защита от вечных цепочек: лимит срабатываний, дальше варнинг.
7. Консоль без окна завершается, когда тихо; код после `как только` выполняется раньше тела.
8. Формы `когда` нет специально: ломалась бы о слово `когда-нибудь` в обычном тексте.

---

## 19. v1.2 — ВЕЩАТЬ (события)

Как функции без параметров, `конец` не пишем:

```
вещать всем какашка
Когда будет получено какашка
задать какашка истина
```

Формы: `вещать всем X` или коротко `вещать X`; обработчик `когда будет получено X` или `когда получено X`.

Правила:
1. Сообщение — любое выражение (текст, `"переменная"`, склейка). Совпадение точное.
2. Обработчики — в конце файла: всё после заголовка — тело до следующего `Когда` или конца файла.
3. Срабатывают все подходящие по порядку, сразу и друг за другом; потом поток продолжается после `вещать`.
4. Без совпадений — тишина, без ошибки.
5. Вложенность можно: `вещать` внутри обработчика зовёт других. Круговая рассылка пропускается с варнингом (см. §23).
6. `Когда` — только на верхнем уровне, не внутри `если`/`повтори`.

---

## 22. v1.5 — ПРОВЕРКА

Проверка кода без запуска. В корне проекта лежит `компиляция.bat` —
ищет все `.ncode` (включая подпапки) и проверяет каждый:

```
компиляция.bat
sub\плохой.ncode:2: `б` уже есть — используй `изменить`
sub\плохой.ncode:3: нет переменной "неттакой"
Проверено файлов: 2, с ошибками: 1
```

Путь — от корня проекта (не с диска), дальше строка и ошибка.
Один файл вручную: `Ncode --проверить игра.ncode`.

Проверяется: неизвестные команды, имена, `задать`/`изменить`,
выражения, структура `если/повтори/событий`, пути картинок и звуков.
`ждать` не ждёт, `спросить` не спрашивает (кладёт `1`), печать молчит —
прогон мгновенный. Проверяются все ветки и все обработчики, даже
невызываемые. Проверка строгая: запуск — истина в последней инстанции.

## 23. v1.6 — ЗАЦИКЛИВАНИЕ РАЗРЕШЕНО

Зацикливание не убивает программу: вместо смерти — один варнинг,
и поток продолжается мимо цикла.

```
Когда будет получено круг
вещать всем круг
```

- Перед запуском игра всегда сначала проверяется молча; есть ошибки —
  запуска нет. Нашлось зацикливание — компиляция выведет ровно 1 варнинг:
  `файл:строка: Варнинг: зацикливание есть` (самоповтор `вещать` в обработчике).
- В запуске зациклившееся место пропускается с варнингом в консоль.
- Хитрые круги через переменные статикой не ловятся — их ловит рантайм-варнинг.

---

## 20. v1.3 — КОНЕЦ МОЖНО НЕ ПИСАТЬ

Если вся конструкция в одну строку — `конец` не нужен:

```
если да то вывести а
если нет то вывести а иначе вывести б
повтори 2 раза вывести а
как только да то вывести а
```

Правила:
1. Работает, только если всё на одной строке. Растянул на несколько — `конец` обязателен, без него ошибка.
2. Следующая строка — уже главный поток: `если да то вывести а` + `напечатать дальше` — дальше выполнится всегда.
3. Вложенность в одну строку можно: `если да то повтори 2 раза вывести а`.

---

## 16. v0.9 — ЗНАКИ

Обычные знаки — то же, что слова:

| Знак | Значит | Знак | Значит |
|------|--------|------|--------|
| `+` | `плюс` | `=` `==` | `равно` |
| `-` | `минус` | `!=` | `неравно` |
| `*` | `умножить` | `>` | `больше` |
| `/` | `разделить` | `<` | `меньше` |
| `%` | `остаток` | `>=` `<=` | `больше/меньше или равно` |
| `&&` | `и` | `\|\|` | `или` |

```
напечатать 5+3        # 8
напечатать 3*4        # 12
напечатать 5+3==8     # истина
напечатать "цена"-"скидка"  # работает без пробелов
напечатать 5--3       # 8 (второй минус — знак числа)
```

Правила:
1. Пробелы не обязательны: `5+3` и `5 + 3` — одно и то же.
2. Ссылки `"..."` не трогаем: `"а+б"` — переменная, а не сумма.
3. Дроби и склейка целы: `3.14`, `..` — знаки их не режут.
4. Одиночные `!`, `&`, `|` — обычный текст (`ура!`, `Том&Джерри`).
5. Изменение (честно): текст со знаками теперь считается: `a+b` — ошибка, `a=b` — сравнение (ложь), `12/05` — деление. Исключения — текст: `буква/буква` без пробелов (`очки/рекорд`, `https://` внутри адресов) остаётся текстом, `://` не комментарий. Лечится будущими точными литералами `'...'`.

---

## 18. v0.11 — ФОРМУЛЫ

Формула — значение там, где значение ждут: в `задать`, `напечатать`, условиях, арифметике.

| Формула | Пример | Ответ |
|---------|--------|-------|
| `случайно от до` | `случайно 1 5` | целое от 1 до 5 |
| `корень x` | `корень 9` | `3` |
| `модуль x` | `модуль -5` | `5` |
| `округлить x` | `округлить 2.5` | `3` |
| `степень a b` | `степень 2 10` | `1024` |
| `минимум a b` | `минимум 3 7` | `3` |
| `максимум a b` | `максимум 3 7` | `7` |
| `длина x` | `длина "слово"` | `6` |

```
задать кубик случайно 1 5
напечатать "кубик"
напечатать случайно 1 5 плюс 10
напечатать корень 16 равно 4   # истина
если случайно 1 10 больше 5 то вывести повезло иначе вывести не повезло конец
```

Правила:
1. Аргументы — числа или `"переменные"` (границы `случайно` — целые).
2. Формула читается с начала куска: `выпало случайно 1 5` без `..` — просто текст.
3. `случайно` при каждом вычислении кидает заново.
4. Служебные слова (нельзя как имена): `случайно корень модуль округлить степень минимум максимум длина`.

---

## 13. v0.7 — ПОВТОРИ

Цикл. Формат: `повтори <число> раз`, тело — команды до `конец`.

```
повтори 3 раза
напечатать привет
конец
```

Словоформы команды: `повтори`, `повторить`, `повторять`. Счёт: `раз`, `раза`, `разов`.

```
повтори 2 раза напечатать быстро конец
задать мало 2
повторить "мало" раз
напечатать из переменной
конец
```

Правила:
1. Счёт — целое неотрицательное число, можно из переменной. `0 раз` — тело пропускается.
2. Вложенность можно: `повтори` внутри `повтори` и `если`, `если` внутри `повтори`.
3. Первая ошибка в теле останавливает повторы.
4. Максимум — 100000 повторов (защита от зависания).
5. Служебные слова (нельзя как имена): `повтори повторить повторять`.
6. Ограничение: голое слово `повтори` в тексте однострочного `если` с `иначе` — только через блочную форму. Однострочный `повтори` внутри однострочного `если` — работает: `если да то повтори 2 раза вывести а конец конец`.

---

## 24. v1.7 — СПИСКИ

Списки нумеруются с 1.

```
создать список очки
добавить в список очки 5
добавить в список очки 10
напечатать длина списка очки   # 2
напечатать взять из списка очки 1   # 5
напечатать есть в списке очки 10    # истина
убрать из списка очки 1
очистить список очки
```

Правила:
1. `создать список` — только новый (дубль — ошибка); значение — любое выражение.
2. `взять из списка ИМЯ НОМЕР`, `длина списка ИМЯ` — работают везде, где значение.
3. `есть в списке ИМЯ ЗНАЧЕНИЕ` — истина/ложь (регистр не важен).
4. Номер мимо диапазона — ошибка с подсказкой.
5. Служебные слова: `список списка списке списку списком взять есть`.

---

## 25. v1.7 — ПОКА, ВСЕГДА, СТОП, ДАЛЬШЕ

```
задать счёт 0
пока "счёт" меньше 5
изменить счёт "счёт" плюс 1
конец
```

```
повторяй
напечатать тик
ждать 1 секунду
если да то стоп конец
конец
```

Правила:
1. `пока` / `покуда` — только блоком (однострочного нет): условие, команды, `конец`.
2. `повторяй` / `всегда` — бесконечно; выход только через `стоп`.
3. `стоп` — выйти из ближайшего цикла; `дальше` — следующий виток.
4. `стоп`/`дальше` вне цикла — ошибка.
5. У `пока` лимит 100000 витков (защита от зависания).
6. Служебные слова: `пока покуда всегда повторяй стоп дальше`.

---

## 26. v1.7 — СИНУС, КОСИНУС, ВРЕМЯ

```
напечатать синус 90    # 1
напечатать косинус 60  # 0.5
задать старт время
```

Правила:
1. Углы в градусах.
2. `время` без аргументов — секунды с запуска (дробное).

---

## 27. v1.8 — ФАЙЛЫ

```
записать в файл saves/очки.txt 100
напечатать прочитать файл saves/очки.txt   # 100
добавить в файл saves/очки.txt 200
```

Правила:
1. Каждый вызов пишет одну строку (`\n` добавляется сам). `записать` затирает файл, `добавить` дописывает.
2. Путь — первым словом без пробелов; недостающие папки создаются сами.
3. `прочитать файл` — значение везде (конец строк при чтении срезается).
4. В проверке `записать/добавить` только проверяются (файл не трогают); `прочитать` несуществующего, но записанного выше — даёт `1`.

---

## 28. v1.8 — НАЙТИ, ЗАМЕНИТЬ

```
напечатать найти мир в привет мир   # 8 (позиция с 1, нет — 0)
напечатать заменить мир на луна в привет мир   # привет луна
напечатать длина привет   # 6
```

Правила:
1. Слова-аргументы — литералы (`мир`), `"переменная"` и числа тоже можно.
2. Последнее значение жадное: `найти о в кот и пёс` — ищет в `кот и пёс`.
3. Регистр важен; `заменить` меняет ВСЕ вхождения.
4. Точные пробелы и тексты со служебными словами — через переменную.
5. Служебные слова: `найти заменить`.

---

## 29. v1.9 — ВЕЩАТЬ С ДАННЫМИ

```
вещать старт данные 42
Когда будет получено старт
задать получил данные
```

Правила:
1. `вещать X данные Y`: обработчики `X` видят `Y` через слово `данные`.
2. Без `данные` — внутри пусто (старое стирается).
3. `данные` — служебное слово, переменную так не назвать.
4. Обработчики — в конце файла: строки главного потока между заголовками впитаются в тело!

---

## 30. v1.9 — СУММА, СРЕДНЕЕ И СПИСОЧНЫЕ МИН/МАКС

```
напечатать сумма списка числа     # 27
напечатать среднее списка числа   # 9
напечатать минимум списка числа   # 4
напечатать максимум списка числа  # 7
```

Правила:
1. Нечисло в списке — ошибка; пустой список: сумма 0, остальное — ошибка.
2. Служебные слова: `сумма среднее`.

---

## 31. v2.0 — ОКНО И РИСОВАНИЕ

```
создать окно 800 600 mygame
масштабирование окна истина
нарисовать герой -100 50
задать размер 100 объекту герой
задать образ объекту герой герой.png
задать цвет 0 255 0 объекту герой
скрыть герой
показать герой
закрыть окно
```

Правила:
1. `Создать окно [ШИРИНА ВЫСОТА НАЗВАНИЕ]` / `открыть окно` (дефолт 800×600 `mygame`, окно одно).
2. `Нарисовать/рисовать/рисуй [ИМЯ] X Y`: первое слово не число → имя; повтор с тем же именем двигает объект; центр 0,0 (X вправо, Y вверх); новое — квадрат 100 чёрный; без имени — один общий квадрат.
3. Размер, поворот/угол, цвет, прозрачность, форма, слой, скорость — через `задать ... объекту <имя>`; картинка — через `задать образ объекту <имя> <путь>` или `добавить костюм`. Синоним: `задать текстура` / `задать текстуру` — то же самое, объект не пересоздаётся (позиция, размер и остальное сохраняются), можно писать путём вперёд: `задать текстуру офис.png объекту какашка`.
4. Картинки (png/jpg/bmp/gif) рядом со скриптом; нет файла — ошибка.
5. Голые `показать/скрыть/идти/двигать` — по последнему нарисованному (без него — ошибка).
6. Без окна рисовать нельзя; `закрыть окно` = конец программы.
7. `Масштабирование окна истина/ложь` (можно `1`/`0`, по умолчанию `ложь`):
```
масштабирование окна ложь   # окно фиксировано, развернуть нельзя
масштабирование окна истина # можно тянуть и жать на весь экран
масштабирование окна 1      # то же что истина
масштабирование окна 0      # то же что ложь
```
`1` = `истина`, `0` = `ложь` (также понимаем `да/нет`, `правда/неправда`). Всё остальное — ошибка. Работает и до, и после `создать окно`.
8. Автомасштаб (когда `масштабирование окна истина`): `создать окно 800 600` задаёт логический размер. Кнопка развернуть / растягивание окна не ломает игру — всё масштабируется само с сохранением пропорций: `масштаб = минимум(ширина_окна/800, высота_окна/600)`, сцена по центру, по бокам чёрные поля. Координаты мыши, касания, края (`край`, `касается`) тоже пересчитываются в логические 800×600, код менять не надо.

---

## 32. v2.1 — ПЕРЕМЕННАЯ НА ЭКРАНЕ

```
задать счёт 5
показать переменную счёт 200 160 60 100 255 255 255
задать счёт 50   # на экране тоже 50
скрыть переменную счёт
```

Формат: `показать переменную <ИМЯ> x y размер прозрачность r g b` — живой HUD над сценой. `скрыть переменную` прячет, `очистить` снимает все. Значение читается каждый кадр.

---

## 33. v2.2 — НЕ И ЖДАТЬ ПОКА

```
если не "готово" то вывести ждём конец
ждать пока "готово" равно да   # формы: пока, покуда, до
```

`не` — выше `и`, ниже сравнений. Срабатывает только перед условием (`не да`, `не "флаг"`, `не 5 равно 5`, `не случайно ...`). Обычный текст `не повезло` остаётся текстом.

`ждать пока <условие>` висит, пока ложь, и качает события, так что обработчик может перевернуть флаг.

---

## 34. v2.2 — ПЕРО, КОСТЮМЫ, СЛОИ, ФОН

```
опустить перо объекту герой
цвет пера красный объекту герой
размер пера 3 объекту герой
поднять перо объекту герой

добавить костюм объекту герой 2.png
следующий костюм объекту герой
предыдущий костюм объекту герой   # или прошлый
костюм 1 объекту герой
задать фон белый
наверх герой
выше герой
вниз герой
ниже герой
```

Голые формы без `объекту` — по последнему. Цвета словом: красный зелёный синий белый чёрный жёлтый оранжевый фиолетовый розовый серый голубой.

---

## 35. v2.2 — ЗАПРОСЫ

```
напечатать икс герой     # x/х синонимы
напечатать игрек герой   # y/у синонимы
напечатать размер герой
напечатать угол герой
напечатать виден герой
напечатать прозрачность герой
напечатать форма герой
напечатать скорость герой
напечатать свойство х объекта герой
напечатать касается герой стена
напечатать касается герой край
напечатать расстояние герой стена
напечатать нажата пробел
напечатать отпущена пробел
```

`отпущена` = клавиша не нажата (`отпущен`/`отпущено` — то же). Формы: `нажата пробел` или `нажата клавиша пробел`, `отпущена a` или `отпущена клавиша a`.

`свойство` знает: х/y, размера/ширины/высоты, угла, костюма, прозрачности, красного/зелёного/синего, видимости, текста, пера, слоя, формы, скорости. Падежи и `обьекта`/`объекта` неважны.

---

## 36. v2.2 — КЛОНЫ, ТАЙМЕРЫ, ПРОЦЕДУРЫ, СКОРОСТЬ

```
когда создан клон герой
вывести клон готов

создать клон герой
удалить герой2

каждые 2 секунды
вывести тик

чтобы прыжок
вывести прыг

вызвать прыжок

задать скорость 100 объекту мяч
двигать объекту мяч
отскочить от края объекту мяч
перемешать список очки
```

Клон — `герой2`, `герой3`… Рекурсия процедур упирается в 100 и даёт варнинг.

---

## 37. v2.2 — ФАЙЛЫ РАСШИРЕНЫ

```
создать папку моисевы
записать в файл saves/очки.txt 100
если есть ли файл saves/очки.txt то вывести есть конец
удалить файл saves/очки.txt
если эсть в списке очки 5 то вывести есть конец
```

Формы: `есть ли файл` = `есть файл` = `эсть`/`ест`/`имеется` (любой вариант). Папки — только в `saves/`, `..` запрещён.

---

## 37б. v2.4 — ИНТЕРНЕТ (GET, POST, PUT, DELETE, PATCH)

```
запрос гет https://example.com сохранить в ответ
напечатать "ответ"
напечатать код ответа

запрос пост https://httpbin.org/post данные привет сохранить в ответ
если код ответа равно 200 то напечатать получилось иначе напечатать не вышло конец

задать тело {"имя": "Барсик"}
запрос пут https://httpbin.org/put данные "тело" сохранить в ответ
запрос делит https://httpbin.org/delete сохранить в ответ
запрос патч https://httpbin.org/patch данные правка сохранить в ответ
```

Правила:
1. Метод — `гет/get`, `пост/post`, `пут/put`, `делит/delete`, `патч/patch` (регистр не важен).
2. Адрес — только `http://` или `https://`, можно из переменной: `запрос гет "адрес" сохранить в ответ`.
3. `данные` — тело запроса (можно склейку `..` и `"переменные"`); для гет/делит обычно опускается. Тело с `{` уходит как JSON, остальное — как текст.
4. Ответ (текст) кладётся в переменную, код (200, 404…) — в `код ответа`. Ошибка сервера (404/500) — не ошибка программы: тело всё равно сохраняется, проверяй `код ответа`.
5. Нет интернета/адреса/ответа дольше 30 секунд — ошибка.
6. В проверке (`компиляция.bat`) сеть не трогается: в переменную кладётся `1`, код — `200`.

**Firebase Realtime Database** (адрес вида `https://моя-игра.firebaseio.com`, схему можно не писать):

```
записать в базу https://моя-игра.firebaseio.com очки/рекорд 100
задать рекорд 150
записать в базу "адрес" "путь" "рекорд"
прочитать базу https://моя-игра.firebaseio.com очки/рекорд сохранить в рекорд
напечатать "рекорд"
удалить из базы https://моя-игра.firebaseio.com очки/рекорд
```

Правила:
1. `записать в базу <адрес> <путь> <значение>` — положить (PUT, перезаписывает). Адрес, путь и значение — текстом или `"переменными"`. Путь — он же ключ (`очки/рекорд`, можно кириллицей — закодируется сам), значение — текст, число (`100`) или `истина`/`ложь` (уйдут как JSON `100`/`true`/`false`).
2. `создать в базе <адрес> <путь> <значение>` — положить, только если пути ещё нет; если есть — ошибка (а `записать в базу` перезапишет).
3. `прочитать базу <адрес> <путь> сохранить в <имя>` — забрать (GET) в переменную. JSON-строка превращается в текст (кавычки снимаются), `true`/`false` — в `истина`/`ложь`, `null`/пусто — в пусто, число и объект — как есть.
4. `удалить из базы <адрес> <путь>` — стереть (DELETE).
5. Адрес и путь можно менять местами — движок угадывает адрес по точке (`прочитать базу очки/рекорд https://моя-игра.firebaseio.com сохранить в рекорд` — тоже работает).
6. Нет пути — ошибка `null`; закрытая правилами база (код 401/403) — ошибка с подсказкой. Для старта годятся открытые правила test mode, закрытым базам с токеном — следующим шагом.
7. В проверке сеть не трогается: запись/создание/удаление только проверяются, чтение кладёт `1`.

---

```
играть звук 1.mp3          # или музыку, включить
играть звук и ждать 1.mp3
остановить звук 1.mp3      # или выключить
пауза звук 1.mp3
продолжить звук 1.mp3
задать громкость 60 для звука 1.mp3
```

Форматы: `.mp3` `.wav` `.m4a` `.ogg`. Громкость 0..100. Синонимы: `музыка`/`музыку`, `включить`/`выключить`.

Фоновая музыка отдельно от эффектов:

```
играть музыку фон.mp3    # крутится по кругу, одна (новая сменяет старую)
остановить музыку        # или остановить музыку фон.mp3
```

Эффектов (`играть звук`) сразу максимум 10 — лишние пропускаются. Музыка в десятку не входит. `и ждать` с музыкой нельзя (она бесконечная).

---

## 39. v2.2 — IDE

`Ncode --ide` — редактор с номерами строк, тёмной темой, подсветкой, живым обновлением словаря, круговой палитрой `Цвета` (клик — RGB в буфер), деревом файлов, консолью вывода. `Новый` сразу спрашивает имя и папку (папка ncode), проводник — нативный. Запуск — в док IDE: вывод, ошибки и варнинги с переходом по строке, поле ввода для `спросить`, кнопка Стоп. 2D-игры открывают окно рядом, консоль не плодит CMD.

---

## 40. Совместимость

Старые однострочные `нарисовать ИМЯ X Y РАЗМЕР ПОВОРОТ R G B ПРОЗРАЧНОСТЬ ПУТЬ` больше не поддерживаются — разноси на отдельные `задать`. Остальное совместимо.



---

## 41. v2.2 — ТИП ДВИЖЕНИЯ

\задать тип движения динамичный объекту игрок
задать тип движения статичный объекту земля
\
\динамичный\ (\дин\, \динамика\, \динамический\) — двигается, \статичный\ (\стат\, \статика\, \статический\) — стоит. Дефолт динамичный. Динамика отталкивается от статики и может стоять на ней. Запрос \свойство тип объекта X\ или \свойство тип движения объекта X\ вернёт \динамичный\/\статичный\.


---

## 42. v2.3 — ФИЗИКА МИРА И ТЕЛ

**Мир:**
\задать гравитацию 0 -9.8
задать плотность воздуха 0.1
\\гравитация x\ / \гравитация y\ — значения.

Единицы: скорость — px/сек, угловая — град/сек, гравитация — м/с² (1м = 100px, то есть `0 -9.8` = земная ≈ -980 px/с²). Коллайдер реально работает: касания, земля и выталкивание считаются по нему, а не по картинке; без коллайдера — по размеру. Триггер не толкается и не держит (только события). Столкновение гасит скорость в стену (упругость 0 = встать, 1 = отскочить), трение тормозит на земле — игроку на `задать скорость` ставь трение 0. Быстрые тела (пули) — `задать пулевой режим истина`, движок режет шаг и не даёт пролететь сквозь тонкое.

**Силы:**
\приложить силу 10 0 объекту шар
приложить импульс 5 5 объекту шар
приложить силу в точке 1 1 по координатам 0 0 объекту шар
приложить крутящий момент 5 объекту шар
притянуть объект шар к объекту куб с силой 10
остановить все силы объекту шар
\
**Тело:**
\сделать тело динамическим объекту шар
сделать тело статическим объекту куб
сделать тело кинематическим объекту платформа
задать массу 5 объекту шар
задать упругость 0.8 объекту шар
задать трение 0.5 объекту шар
задать сопротивление воздуха 0.1 объекту шар
задать сопротивление вращению 0.1 объекту шар
зафиксировать вращение ложь объекту шар
задать масштаб гравитации 1 объекту шар
задать пулевой режим ложь объекту шар
\\масса объекта шар\ — значение.

**Скорость:**
\задать скорость 10 5 объекту шар
задать угловую скорость 10 объекту шар
телепортировать 0 0 объекту шар
\\скорость x объекта шар\, \общая скорость объекта шар\, \угловая скорость объекта шар\.

**Коллайдер:**
\задать коллайдер коробка 50 50 объекту куб
задать коллайдер круг 25 объекту шар
задать коллайдер капсула 20 40 объекту шар
задать смещение коллайдера 5 5 объекту шар
сделать триггером истина объекту шар
\\объект шар является триггером\ — условие.

**Суставы:**
\соединить веревкой шар и куб длина 100
соединить пружиной шар и куб жесткость 10 гашение 1
соединить шарниром шар и куб в точке 0 0
ограничить угол шарнира -45 45 соединению j1
задать мотор шарнира 10 сила 100 соединению j1
задать прочность соединения 1000 соединению j1
удалить соединение j1
\
**События:**
\когда происходит столкновение шар и куб
когда объект шар входит в триггер куб
объект шар на земле
сила последнего удара объекта шар
точка столкновения x
бросить луч от 0 0 до 100 100 пересекает куб
дистанция луча до куб от 0 0 в направлении 45
\

**Клавиши:**
\когда нажата клавиша a
задать скорость 180 0 объекту игрок
когда отпущена клавиша a
задать скорость 0 0 объекту игрок
\
Слово `клавиша` можно пропускать (`когда нажата a`, `когда отпущена пробел`). Имя — любое: `a`, `d`, `пробел`, `влево`, `ввод`… Русские буквы тоже можно (`ф` = физическая A), движок понимает обе раскладки и на нажатии, и на отпускании (на Андроиде клавиатуры нет — там касания: `при нажатии`). Важно: `когда`-обработчики ставь в конце файла, после всех `как только`/`если`/`повтори` — иначе следующий блок впитается в тело обработчика. Пример движения с двумя клавишами:
\когда нажата клавиша a
задать скорость -180 0 объекту игрок
когда нажата клавиша d
задать скорость 180 0 объекту игрок
когда отпущена клавиша a
если нажата d то задать скорость 180 0 объекту игрок иначе задать скорость 0 0 объекту игрок конец
когда отпущена клавиша d
если нажата a то задать скорость -180 0 объекту игрок иначе задать скорость 0 0 объекту игрок конец
\

---

## 43. Комментарии

```
# комментарий на всю строку
// тоже комментарий
задать а 5 // комментарий после кода
задать б 10 # комментарий после кода
напечатать "а" // комментарий работает везде
если "а" равно 5 то // комментарий в заголовке блока
конец // комментарий после конца
когда нажата клавиша пробел // комментарий в обработчике
```

`#` и `//` — комментарии до конца строки. Внутри кавычек `"..."` не режутся: `"привет # мир // тест"` остаётся текстом. Поддерживаются везде: в блоках `если/повтори/пока/повторяй`, в обработчиках `когда/при нажатии`, в параметрах. Исключение: `://` в адресах — не комментарий (`https://example.com` работает).

---

## 44. v2.5 — КАМЕРА

```
нарисовать герой 500 0
следить за объектом герой истина
```

Камера ставит объект в центр и едет за ним (мир больше окна — платформер с длинным уровнем). Край считается от камеры, мышь и касания пересчитываются сами, переменные-HUD стоят на месте. Коротко: `следить герой истина`. Выключить: `следить за объектом герой ложь` (камера назад в центр, `0`/`1` тоже можно). Если объекта нет — камера стоит где была.

---

## 45. v2.5 — ЗАПУСК СКРИПТОВ (сцены — отдельные .ncode)

```
запустить уровень2.ncode          # выполнить поверх: сцена, переменные и скрипты живут дальше
запустить уровень2.ncode ложь     # то же самое
запустить меню.ncode истина       # стереть всё, остановить всех и начать другой скрипт
```

Правила:
1. Путь — рядом со скриптом (можно в подпапку: `запустить уровни/2.ncode`), картинки и звуки дочернего ищутся рядом с ним.
2. Без стирания дочерний видит переменные родителя и добавляет свои обработчики (`когда`, `каждые`).
3. Со стиранием (`истина`) строки родителя после `запустить` не выполняются — конец истории.
4. Что трётся, а что нет: объекты, звуки, таймеры, обработчики, физика, камера, фон — в ноль; а переменные и списки ЖИВУТ дальше (было `задать счёт 1` — так `1` и останется в новой сцене).
5. Круг (`а` запускает `б`, а `б` — `а`) — ошибка. В проверке дочерний тоже проверяется.
6. На Андроиде дочерние `.ncode` клади в `assets` рядом с `game.ncode` (или в файлы приложения) — подхватятся сами, картинки/звуки тоже ищутся в `assets`.